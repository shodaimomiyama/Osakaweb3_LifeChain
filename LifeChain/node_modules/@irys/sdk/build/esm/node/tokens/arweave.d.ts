import { Arweave } from "../utils.js";
import type { JWKInterface, Signer } from "arbundles";
import BigNumber from "bignumber.js";
import type { TokenConfig, Tx } from "../../common/types.js";
import { BaseNodeToken } from "../token.js";
import type Transaction from "arweave/node/lib/transaction.js";
import type { NodeIrysConfig } from "../types.js";
import BaseNodeIrys from "../base.js";
export default class ArweaveConfig extends BaseNodeToken {
    protected providerInstance: Arweave;
    constructor(config: TokenConfig);
    private getProvider;
    getTx(txId: string): Promise<Tx>;
    ownerToAddress(owner: any): string;
    sign(data: Uint8Array): Promise<Uint8Array>;
    getSigner(): Signer;
    verify(pub: any, data: Uint8Array, signature: Uint8Array): Promise<boolean>;
    getCurrentHeight(): Promise<BigNumber>;
    getFee(amount: BigNumber.Value, to?: string): Promise<BigNumber>;
    sendTx(data: Transaction): Promise<any>;
    createTx(amount: BigNumber.Value, to: string, fee?: string): Promise<{
        txId: string | undefined;
        tx: any;
    }>;
    getPublicKey(): string;
}
export declare class ArweaveIrys extends BaseNodeIrys {
    constructor({ url, key, config }: NodeIrysConfig<JWKInterface>);
}
